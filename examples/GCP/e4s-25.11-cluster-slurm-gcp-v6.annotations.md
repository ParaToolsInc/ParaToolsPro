1. !!! warning
       Set this to your own GCP project ID, or comment this line and invoke `gcluster` with
       the `--vars project_id="${PROJECT_ID}"` flag instead. The value shown here is the
       ParaTools development project and will not work for you.
2. !!! info
       This must match the image family of the ParaTools Pro for E4S™ marketplace image
       you have subscribed to. The current GCluster (x86-64) image family is
       `paratools-gcluster-e4s-2511-nvidia89-x86-64`.
3. !!! info "Default VPC firewall behavior"
       The [`modules/network/vpc`][vpc-module] module with no `settings:` block creates a
       VPC with two firewall rules by default: one allowing SSH from
       [Identity-Aware Proxy (IAP)][iap] (range `35.235.240.0/20`) and one allowing all
       intra-VPC traffic. The "SSH" button in the GCP Console for the login node uses IAP,
       so it works with this default. If you want to SSH directly to the login node's
       public IP from your workstation, you must add a firewall rule that allows your
       workstation's IP -- see [Allowing direct SSH from your workstation](#allowing-direct-ssh-from-your-workstation).
4. !!! info
       The `machine_type` and `node_count_dynamic_max` set the instance type and the
       maximum number of nodes Slurm can spin up dynamically in this nodeset. Tune these
       to match your usage and quotas (vCPUs = cores-per-node x `node_count_dynamic_max`).
       Note this same setting also appears in the `compute_nodeset` (line 74) and
       `h3_nodeset` (line 93) blocks. For compute-intensive, tightly coupled jobs, C3 or
       H3 instances perform well.
5. !!! info "GPU on the debug nodeset"
       The `debug_nodeset` attaches one NVIDIA Tesla T4 GPU per node by default, providing
       a low-cost path to test CUDA / NeMo / PyTorch GPU workloads before scaling up.
       Set `count: 0` and remove the `guest_accelerator` block, or change to a CPU-only
       instance type, if you do not need a GPU on the debug partition.
6. !!! info
       To access the full [high-speed per-VM Tier_1 networking capabilities][Tier_1 networking]
       on supported instance types, the [gvnic] driver must be enabled. This setting also
       appears in the `h3_nodeset` block (line 98).
7. !!! info "Optional H3 partition"
       This example includes an additional SLURM partition containing H3 nodes. Access to
       H3 instances may require a quota increase request. You do not need multiple SLURM
       partitions, so you may remove the `h3_nodeset` and `h3_partition` modules (and the
       `- h3_partition` reference in `slurm_controller.use`) if you do not have H3 access.

[vpc-module]: https://github.com/GoogleCloudPlatform/cluster-toolkit/tree/main/modules/network/vpc
[iap]: https://cloud.google.com/iap
[Tier_1 networking]: https://cloud.google.com/compute/docs/networking/configure-vm-with-high-bandwidth-configuration
[gvnic]: https://cloud.google.com/compute/docs/networking/using-gvnic
