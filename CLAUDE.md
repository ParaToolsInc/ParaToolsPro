# ParaToolsPro docs
Live at <https://docs.paratoolspro.com>

## CFN
Edit `docs/assets/**/*cloudformation*.yaml` → run `uv tool run cfn-lint <file>`. Non-zero = fix. Pre-commit hook enforces same; never bypass.
